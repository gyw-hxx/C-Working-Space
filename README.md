#我的第一个c语言
```c
#include <stdio.h>  // 引入标准输入输出库

int main() {
    printf("Hello, kg\n");  // 输出到屏幕
    return 0;               // 程序正常退出
}
```  

#程序结构
C 语言程序一般经过四个步骤：
1. 编辑（.c 文件）  
2. 编译（生成 .obj 或中间文件）  
3. 链接（生成可执行文件 .exe）  
4. 运行（执行生成的程序）    


